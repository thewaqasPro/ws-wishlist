# Checkout extensions

WS Wishlist does not currently ship a checkout UI extension and does not require checkout scopes. The existing `verifyCheckout` middleware is retained as starter infrastructure only; it is not part of the wishlist runtime or App Store reviewer flow.

Do not add checkout behavior or scopes unless a real checkout-extension feature is implemented, documented, and reviewed separately.
