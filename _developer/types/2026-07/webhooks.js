/**
 * Minimal webhook payload types used by this app for Shopify API version
 * 2026-07. Add focused typedefs here only when a runtime handler needs them.
 */

/**
 * @typedef {Object} APP_UNINSTALLED
 * @property {number|string} id
 * @property {string} name
 * @property {string} email
 * @property {string|null} domain
 * @property {string|null} myshopify_domain
 * @property {string|null} updated_at
 */

export {};
